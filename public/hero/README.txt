Coloque aqui as imagens do Hero.
Nomes esperados no codigo (preferencialmente em .webp, 1920px de largura):
- hero-1.webp
- hero-2.webp
- hero-3.webp
- hero-4.webp

Use versões leves (já otimizadas) para evitar travar o carregamento do banner.

Arquivos dentro de `public` ficam acessiveis em `/hero/...` no navegador (Vite serve direto sem import).
